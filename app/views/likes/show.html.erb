<h1>Likes by</h1>
<table>
	<tr>
		<th>profile</th>
		<th>name</th>
	</tr>
	<% @users.each do |user| %>
	<tr>
		<td><%= image_tag user.profile_image.url, style: 'height:1.5vw;width:3vw;' if  user.profile_image?%></td>
		<td><%= user.name %></td>
	</tr>
	<% end %>
</table>